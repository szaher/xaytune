# Missing / Incomplete Features

Last updated: 2026-06-03 15:00

| ID | Feature / Requirement | Where It Should Appear | Current Behavior | Gap | Evidence | Priority | Acceptance Criteria | Complexity |
|----|----------------------|----------------------|-----------------|-----|----------|----------|-------------------|------------|
| GAP-001 | GGUF conversion must use correct tool | `export/gguf.py` | Shells out to `python -m llama_cpp.convert` which doesn't exist | Conversion always fails with ModuleNotFoundError | `export/gguf.py:32-36` | P1 | Given llama.cpp tools installed, When `to_gguf()` called, Then GGUF file produced | M |
| GAP-002 | Hub push should warn when tokenizer missing | `export/hub.py` | Silently skips tokenizer upload when model object passed without tokenizer | Unusable model published to Hub | `export/hub.py:33-38` | P2 | Given model object without tokenizer, When `push_to_hub()` called, Then warning emitted | XS |
| GAP-003 | Config validation must run from Python API and Studio | `recipes/base.py` | `validate_config()` only called from CLI (`cli.py:303`) | Invalid configs skip validation when using Python API or Studio | `config/validation.py`, `recipes/base.py` | P1 | Given invalid config via Python API, When `setup_training()` called, Then ConfigValidationError raised | S |
| GAP-004 | Pretrain recipe needs validation rules | `config/validation.py` | Zero validation for `recipe="pretrain"` | `recipe="pretrain", method="lora"` accepted silently | `config/validation.py` | P2 | Given `recipe="pretrain", method="lora"`, When `validate_config()`, Then error raised | S |
| GAP-005 | Config overrides must reject unknown keys | `config/parser.py` | `apply_overrides()` silently creates new keys for typos | `trainer.lerning_rate=1e-4` accepted, default used instead | `config/parser.py:42-52` | P1 | Given override `trainer.lerning_rate=1e-4`, When `apply_overrides()`, Then ValueError with suggestion | S |
| GAP-006 | Optional dependency imports need helpful errors | `models/peft.py`, `logging/*.py` | Raw `ImportError` when optional deps missing | User gets cryptic traceback instead of install instructions | `models/peft.py:3`, `logging/wandb.py:7` | P1 | Given peft not installed, When `finetune(method="lora")`, Then error says "pip install xaytune[peft]" | S |
| GAP-007 | Eval callback must compute non-loss metrics | `trainer/eval_callback.py` | Non-loss metrics get `compute_fn([], [])` — always 0 | `token_accuracy` during training always shows 0 | `trainer/eval_callback.py:59-64` | P1 | Given `EvalConfig(metrics=["token_accuracy"])`, When eval runs during training, Then nonzero result | S |
| GAP-008 | Checkpoint loading must handle cross-device | `trainer/checkpointing.py` | No `map_location` on `torch.load()` calls | GPU checkpoint fails to load on CPU | `trainer/checkpointing.py:72-90`, `trainer/loop.py:108-114` | P1 | Given GPU checkpoint, When loaded on CPU, Then no error | S |
| GAP-009 | Checkpoint save must handle tensor metrics | `trainer/checkpointing.py` | `json.dumps(state.metrics)` may contain tensors | `TypeError` during save, checkpoint lost | `trainer/checkpointing.py:~47` | P1 | Given `state.metrics={"loss": torch.tensor(0.5)}`, When checkpoint saved, Then `metadata.json` written | XS |
| GAP-010 | Studio must wire alignment loss for align recipes | `studio/app.py` | Studio calls `setup_training()` + `trainer.train()` directly | DPO/GRPO training from Studio uses wrong loss | `studio/app.py` | P1 | Given Studio config with `recipe="align"`, When training launched, Then alignment loss used | S |
| GAP-011 | Format dropdown must match format registry | `studio/app.py` | Lists "completion", "pretrain" which don't exist | Studio users get KeyError selecting those formats | `studio/app.py:~343` | P1 | Given Studio opened, When format dropdown clicked, Then shows: alpaca, sharegpt, chat, text, preference | XS |
| GAP-012 | CLI eval must default --metrics | `cli.py` | `args.metrics` is None, `.split()` crashes | `xaytune eval --model X --dataset Y` crashes | `cli.py:~387` | P1 | Given `xaytune eval` without `--metrics`, Then defaults to loss,perplexity | XS |
| GAP-013 | model_merge must output config.json | `export/model_merge.py` | Only saves `pytorch_model.bin` | Output not loadable by `from_pretrained()` | `export/model_merge.py:158-162` | P1 | Given model_merge completes, Then output dir contains config.json | S |
| GAP-014 | SFT must mask prompt tokens for instruction formats | `data/tokenizer.py` | `labels = list(input_ids)` for full sequence | Model trains on prompt, wasting capacity | `data/tokenizer.py:62` | P0 | Given alpaca sample, When tokenized, Then labels=-100 for prompt tokens | M |
| GAP-015 | Preference log-probs must exclude prompt | `recipes/align/logprobs.py` | `get_sequence_logps()` sums over full sequence including prompt | Alignment signal diluted | `recipes/align/logprobs.py:24-26` | P0 | Given preference pair, When log-probs computed, Then only response tokens counted | M |
| GAP-016 | QLoRA must call prepare_model_for_kbit_training | `models/peft.py` | `get_peft_model()` called directly on 4-bit model | Unstable training, possible dtype errors | `models/peft.py:46` | P0 | Given 4-bit model, When apply_lora(), Then prepare_model_for_kbit_training() called first | S |
| GAP-017 | DeepSpeed must use engine backward/step | `trainer/loop.py` | Vanilla `loss.backward()` / `optimizer.step()` used | No ZeRO benefits, wasted GPU resources | `trainer/loop.py:220-238` | P0 | Given DeepSpeed engine, When training_step(), Then engine.backward() and engine.step() called | L |
